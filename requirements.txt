# Zariya v2 dependencies
# Install: pip install -r requirements.txt

# Core UI
streamlit>=1.35.0

# Local LLM inference (CPU — works everywhere)
llama-cpp-python>=0.2.80
# GPU (NVIDIA): CMAKE_ARGS="-DLLAMA_CUDA=on" pip install llama-cpp-python --force-reinstall
# GPU (Apple):  CMAKE_ARGS="-DLLAMA_METAL=on" pip install llama-cpp-python --force-reinstall

# Text-to-speech (offline)
pyttsx3>=2.90
